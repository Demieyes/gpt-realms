{
  "name": "gpt-realms",
  "version": "0.3.4",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch-assets": "node scripts/fetch-assets.mjs",
    "build": "npm run fetch-assets && esbuild public/game-clean.js --bundle --format=esm --platform=browser --outfile=public/game-bundle.js",
    "postinstall": "npm run build",
    "start": "node server.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
